---
name: create-project
description: "Create one registered HEINRICH project satellite under Work with a minimal charter, physical asset and skill placeholders, thin runtime pointers, bridge and first task. Use only for a new independent project, not a task or experiment."
---

# /create-project

Read and follow the complete shared procedure at:

`heinrich/agent_brain/understanding/playbooks/create-project.md` (relative to `Work/`)

## Claude adapter

1. `WORK_ROOT` is the nearest ancestor containing `heinrich/agent_brain/`;
   read root `CLAUDE.md`.
2. Use `projekt-template/` as the visible scaffold reference, substituting
   approved values and copying no stale project facts.
3. Run ownership preflight on every planned satellite file, bridge and
   changelog; re-read shared files immediately before patching.
4. Use `AskUserQuestion` for the Git-repo choice.
5. After bridge validation invoke `/create-task` for the first action; never
   hand-author task frontmatter.
6. Report every path and incomplete phase. No backup, commit, push or deploy.
