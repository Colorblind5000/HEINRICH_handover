---
name: create-project
description: "Create one registered HEINRICH project satellite under Work with a minimal charter, physical asset and skill placeholders, thin runtime pointers, bridge and first task. Use only for a new independent project, not a task or experiment."
---

# $create-project

Read and follow the complete shared procedure at:

`heinrich/agent_brain/understanding/playbooks/create-project.md`

## Codex adapter

1. Find the nearest ancestor containing `heinrich/agent_brain/`; call it
   `WORK_ROOT`, and read root `AGENTS.md`.
2. Use `projekt-template/` as the visible scaffold reference, but substitute
   the approved project values; never copy stale project-specific facts.
3. Apply dirty-file preflight to every planned satellite file, bridge and
   changelog before its first write. Re-read shared files immediately before
   patching.
4. Use the clickable-choice tool for the Git-repo choice when available.
5. After bridge validation invoke `$create-task` for the first action; never
   hand-author task frontmatter.
6. Report every path and any incomplete phase. No backup, commit, push or
   deploy.
