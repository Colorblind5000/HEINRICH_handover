# Placeholder — tasks

Projektets konkrete tasks lever som ét Markdown-kort pr. handling i denne
mappe. `create-project` erstatter denne placeholder med den første task gennem
`create-task`.

Ved manuel brug af templaten slettes `PLACEHOLDER.md`, når første task-kort
oprettes.
